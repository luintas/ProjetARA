package ara.Nodes

import peersim.core.Node
import peersim.core.GeneralNode
class Proposer(str : String) extends GeneralNode( str ) {
    val tmp = new  GeneralNode("Dummy String")
}

class mockAcceptor(str : String) extends GeneralNode( str ) {
    val tmp = new  GeneralNode("Dummy String")
}